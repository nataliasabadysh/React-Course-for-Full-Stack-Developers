
const initState = {
    list: [],
    isLoading: false,
    error: null
}

export const contactReducer = (state = initState, { type, payload }) => {
    switch(type){

        
        default: {
            return state
        }
    }   
}